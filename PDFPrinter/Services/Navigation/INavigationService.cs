using PDFPrinter.ViewModels;
using System;
using System.Threading.Tasks;

namespace PDFPrinter.Services.Navigation
{
    public interface INavigationService
    {
        //Task InitializeAsync();

        //Task NavigateToAsync<TViewModel>() where TViewModel : ViewModel;

        //Task NavigateToAsync<TViewModel>(object parameter) where TViewModel : ViewModel;

        //Task NavigateToAsync(Type viewModelType);

        //Task NavigateToAsync(Type viewModelType, object parameter);

        //Task NavigateBackAsync();

        //Task RemoveLastFromBackStackAsync();

        //Task GoToViewModel(string destination);

        //Task BackToMenuPrincipal();
    }
}
